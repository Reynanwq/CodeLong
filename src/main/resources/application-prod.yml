spring:
  data:
    mongodb:
      uri: ${CODELONG_MONGO_URI} # obrigatorio

codelong:
  jwt:
    secret: ${CODELONG_JWT_SECRET} # obrigatorio
    expiration: ${CODELONG_JWT_EXPIRATION:2h}
  cors:
    allowed-origins: ${CODELONG_CORS_ALLOWED_ORIGINS:}

logging:
  level:
    com.codelong: INFO